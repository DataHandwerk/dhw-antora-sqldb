= sys_self

== Description

this schema is not required when connecting the repository to a "normal" dwh database

* it contains some views and procedures similar to objects in schema `repo_sys` to apply similar logic not to the dwh database but to the current database (self)
* is is only used the repository database development

== Objects

include::partial$navlist/navlist-schema-sys_self.adoc[]

